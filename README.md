# kageplay-redirector

Simple TypeScript/Vite landing page with a **KagePlay Home Page** redirect button.

## Run locally

```bash
npm install
npm run dev
```

## Image assets

Place these files in the project root for full visual design:

- `kageplay-bg.jpg` (background image)
- `kageplay-logo.png` (KagePlay logo)
